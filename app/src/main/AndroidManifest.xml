<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"
        android:minSdkVersion="34" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <permission
        android:name="${applicationId}.permission.BIND_LLM_SERVICE"
        android:protectionLevel="signature" />

    <application
        android:name=".NVApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeuroVerse"
        tools:targetApi="34" >
        <activity
            android:name=".activity.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.NeuroVerse"
            android:windowSoftInputMode="adjustResize"
            tools:targetApi="33" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.ModelLoadingActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.NeuroVerse"
            android:windowSoftInputMode="adjustResize"
            tools:targetApi="33" >
        </activity>



        <activity
            android:name=".activity.ModelPickerActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.NeuroVerse"
            android:windowSoftInputMode="adjustResize"
            tools:targetApi="33" >
        </activity>

        <activity
            android:name=".activity.RagActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.NeuroVerse"
            android:windowSoftInputMode="adjustResize"
            tools:targetApi="33" >

            <!-- Intent filter for .bit RAG files -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="content" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.bit" />
                <data android:pathPattern=".*\\.BIT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.RagDataReaderActivity"
            android:exported="false"
            android:theme="@style/Theme.NeuroVerse"
            android:windowSoftInputMode="adjustResize"
            tools:targetApi="33" />

        <activity
            android:name=".activity.DiagnosticsActivity"
            android:exported="false"
            android:theme="@style/Theme.NeuroVerse"
            android:windowSoftInputMode="adjustResize"
            tools:targetApi="33" />

        <service
            android:name=".service.LLMService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync"
            android:permission="${applicationId}.permission.BIND_LLM_SERVICE"
            android:process=":inference"
            android:stopWithTask="false"
            tools:targetApi="29" />

        <service
            android:name=".service.ModelDownloadService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            tools:targetApi="29" />

        <!-- WorkManager's foreground service -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge"
            tools:targetApi="29" />

        <service
            android:name=".service.BitAgentService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            tools:targetApi="29" />



        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>